{
  "name": "express-intercept",
  "description": "Build Express middleware to intercept / replace / inspect / transform response",
  "version": "1.2.3",
  "author": "Yusuke Kawasaki <u-suke@kawa.net>",
  "bugs": {
    "url": "https://github.com/kawanet/express-intercept/issues"
  },
  "contributors": [
    "Yusuke Kawasaki <u-suke@kawa.net>"
  ],
  "dependencies": {
    "async-request-handler": "^1.0.0"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^16.0.3",
    "@rollup/plugin-sucrase": "^5.1.0",
    "@types/express": "^4.17.25",
    "@types/node": "^24.12.2",
    "@types/supertest": "^7.2.0",
    "express": "^4.21.1",
    "middleware-supertest": "^1.1.0",
    "rollup": "^4.60.2",
    "supertest": "^7.2.2",
    "typescript": "^5.9.3"
  },
  "devEngines": {
    "runtime": {
      "name": "node",
      "version": ">=22",
      "onFail": "warn"
    }
  },
  "engines": {
    "node": ">=20"
  },
  "exports": {
    ".": {
      "require": "./dist/express-intercept.cjs",
      "import": {
        "types": "./types/express-intercept.d.ts",
        "default": "./dist/express-intercept.mjs"
      }
    }
  },
  "files": [
    "LICENSE",
    "README.md",
    "dist/express-intercept.cjs",
    "dist/express-intercept.mjs",
    "lib/*.ts",
    "types/*.d.ts"
  ],
  "homepage": "https://github.com/kawanet/express-intercept#readme",
  "keywords": [
    "expressjs",
    "inspector",
    "interceptor",
    "middleware",
    "modify",
    "response",
    "tamper"
  ],
  "license": "MIT",
  "main": "./dist/express-intercept.cjs",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kawanet/express-intercept.git"
  },
  "scripts": {
    "build": "make -C builder",
    "fixpack": "fixpack",
    "prepack": "if node -v | grep -q '^v20'; then echo 'ERROR: packing is not allowed on Node 20'; exit 1; fi",
    "prepare": "if node -v | grep -q '^v20'; then :; else make -C builder clean all test; fi",
    "test": "make -C builder test"
  },
  "sideEffects": false,
  "type": "module",
  "types": "types/express-intercept.d.ts"
}
